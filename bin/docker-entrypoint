#!/bin/bash -e

# Se o comando passado for rodar o servidor, limpa o PID antigo
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  rm -f /rails/tmp/pids/server.pid
fi

# Executa o comando principal do Docker
exec "${@}"